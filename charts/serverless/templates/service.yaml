apiVersion: v1
kind: Service
metadata:
  name: {{ template "jammerwebhost.fullname" . }}
  labels:
    app: {{ template "jammerwebhost.name" . }}
    chart: {{ template "jammerwebhost.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "jammerwebhost.name" . }}
    release: {{ .Release.Name }}
